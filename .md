# 🏦 HƯỚNG DẪN TÍCH HỢP VNPAY - HOÀN CHỈNH

## 📋 TỔNG QUAN

VNPay là cổng thanh toán trực tuyến phổ biến tại Việt Nam. Tích hợp VNPay cho phép khách hàng thanh toán qua:
- Thẻ ATM nội địa
- Thẻ tín dụng/ghi nợ quốc tế
- QR Code
- Ví điện tử

---

## 🗄️ DATABASE - ĐÃ BỔ SUNG

### 1. **Table Mới: `VNPayTransactions`**

Lưu trữ toàn bộ thông tin giao dịch VNPay:

| Column | Type | Mô tả |
|--------|------|-------|
| `TransactionID` | INT | ID giao dịch (PK) |
| `OrderID` | INT | FK đến Orders |
| `vnp_TxnRef` | NVARCHAR(100) | Mã giao dịch unique |
| `vnp_Amount` | BIGINT | Số tiền (VNĐ × 100) |
| `vnp_TransactionNo` | NVARCHAR(100) | Mã GD từ VNPay |
| `vnp_BankCode` | NVARCHAR(50) | Mã ngân hàng |
| `vnp_ResponseCode` | NVARCHAR(10) | Mã phản hồi (00 = thành công) |
| `Status` | NVARCHAR(50) | Pending/Success/Failed/Cancelled |
| `CreatedAt` | DATETIME | Thời gian tạo |
| `CallbackReceivedAt` | DATETIME | Thời gian nhận callback |

### 2. **Stored Procedures Mới:**

#### `sp_CreateVNPayTransaction`
Tạo giao dịch VNPay mới khi khách chọn thanh toán

**Input:**
- `@OrderID` - ID đơn hàng
- `@vnp_TxnRef` - Mã giao dịch (OrderID + Timestamp)
- `@vnp_Amount` - Số tiền (VNĐ × 100)
- `@vnp_OrderInfo` - Mô tả đơn hàng
- `@vnp_IpAddr` - IP khách hàng
- `@RequestUrl` - URL redirect đến VNPay

**Output:**
- `TransactionID` - ID giao dịch vừa tạo

#### `sp_UpdateVNPayTransaction`
Cập nhật kết quả sau khi nhận callback từ VNPay

**Input:**
- `@vnp_TxnRef` - Mã giao dịch
- `@vnp_TransactionNo` - Mã GD từ VNPay
- `@vnp_ResponseCode` - Mã phản hồi
- `@Status` - Success/Failed

**Logic:**
- Nếu `ResponseCode = '00'` → Cập nhật Order thành "Đã thanh toán", trừ kho
- Nếu thất bại → Hủy đơn hàng

#### `sp_GetVNPayTransactionByOrderID`
Lấy thông tin giao dịch theo OrderID

#### `sp_GetVNPayTransactionByTxnRef`
Lấy thông tin giao dịch theo TxnRef (dùng khi verify callback)

#### `sp_GetVNPayTransactionHistory`
Lấy lịch sử giao dịch (cho Admin) với pagination

#### `sp_CancelVNPayTransaction`
Hủy giao dịch khi khách không thanh toán

### 3. **View: `vw_VNPayTransactionSummary`**
View tổng hợp để báo cáo, dashboard

### 4. **Indexes:**
- `IX_VNPayTransactions_OrderID` - Tìm theo OrderID
- `IX_VNPayTransactions_TxnRef` - Tìm theo TxnRef
- `IX_VNPayTransactions_Status` - Lọc theo Status
- `IX_VNPayTransactions_CreatedAt` - Sắp xếp theo thời gian

---

## 🔧 CÁCH CHẠY SQL SCRIPT

### Bước 1: Mở SSMS
```
1. Mở SQL Server Management Studio
2. Connect đến server của bạn
3. Chọn database PC_Store
```

### Bước 2: Chạy Script
```sql
-- Mở file: VNPAY_DATABASE_SETUP.sql
-- Nhấn F5 hoặc Execute
```

### Bước 3: Verify
```sql
-- Kiểm tra table đã tạo
SELECT * FROM INFORMATION_SCHEMA.TABLES 
WHERE TABLE_NAME = 'VNPayTransactions';

-- Kiểm tra stored procedures
SELECT * FROM INFORMATION_SCHEMA.ROUTINES 
WHERE ROUTINE_NAME LIKE 'sp_%VNPay%';
```

---

## 🔐 VNPAY CONFIG (appsettings.json)

Bạn cần thêm config VNPay vào `appsettings.json`:

```json
{
  "VNPay": {
    "TmnCode": "YOUR_TMN_CODE",
    "HashSecret": "YOUR_HASH_SECRET",
    "PaymentUrl": "https://sandbox.vnpayment.vn/paymentv2/vpcpay.html",
    "ReturnUrl": "http://localhost:5187/api/vnpay/callback",
    "Version": "2.1.0",
    "Command": "pay",
    "CurrCode": "VND",
    "Locale": "vn"
  }
}
```

**Lưu ý:**
- `TmnCode` và `HashSecret`: Lấy từ VNPay sau khi đăng ký
- Sandbox URL: Dùng để test
- Production URL: `https://vnpayment.vn/paymentv2/vpcpay.html`

---

## 📊 LUỒNG THANH TOÁN VNPAY

```
1. Khách hàng chọn "Thanh toán VNPay" tại trang Payment
   ↓
2. Backend tạo giao dịch: sp_CreateVNPayTransaction
   ↓
3. Backend tạo URL redirect đến VNPay (có hash)
   ↓
4. Khách hàng thanh toán trên trang VNPay
   ↓
5. VNPay gọi callback về: /api/vnpay/callback
   ↓
6. Backend verify hash và cập nhật: sp_UpdateVNPayTransaction
   ↓
7. Nếu thành công:
   - Cập nhật Order: PaymentStatus = "Đã thanh toán"
   - Trừ kho: StockQuantity -= Quantity
   - Redirect khách đến: /paymentcomplete
   ↓
8. Nếu thất bại:
   - Hủy Order
   - Redirect khách đến: /payment?error=true
```

---

## 🧪 TEST GIAO DỊCH

### Test Data (Sandbox):

**Thẻ ATM:**
- Số thẻ: `9704198526191432198`
- Tên chủ thẻ: `NGUYEN VAN A`
- Ngày phát hành: `07/15`
- Mật khẩu OTP: `123456`

**QR Code:**
- Quét mã QR trên app ngân hàng test

### Test Script:

```sql
-- 1. Tạo giao dịch test
EXEC sp_CreateVNPayTransaction
    @OrderID = 1,
    @vnp_TxnRef = 'ORD1_20260528123456',
    @vnp_Amount = 6232000000,
    @vnp_OrderInfo = N'Thanh toán đơn hàng #1',
    @vnp_IpAddr = '192.168.1.1',
    @RequestUrl = 'https://sandbox.vnpayment.vn/...';

-- 2. Giả lập callback thành công
EXEC sp_UpdateVNPayTransaction
    @vnp_TxnRef = 'ORD1_20260528123456',
    @vnp_TransactionNo = '14398416',
    @vnp_BankCode = 'NCB',
    @vnp_CardType = 'ATM',
    @vnp_PayDate = '20260528123500',
    @vnp_ResponseCode = '00',
    @vnp_TransactionStatus = '00',
    @vnp_SecureHash = 'abc123...',
    @ResponseData = '{"vnp_ResponseCode":"00"}',
    @Status = N'Success';

-- 3. Kiểm tra kết quả
EXEC sp_GetVNPayTransactionByOrderID @OrderID = 1;

SELECT * FROM Orders WHERE OrderID = 1;
SELECT * FROM VNPayTransactions WHERE OrderID = 1;
```

---

## 📈 RESPONSE CODES VNPAY

| Code | Ý nghĩa |
|------|---------|
| `00` | Giao dịch thành công |
| `07` | Trừ tiền thành công, giao dịch bị nghi ngờ |
| `09` | Thẻ chưa đăng ký Internet Banking |
| `10` | Xác thực thông tin thẻ sai quá 3 lần |
| `11` | Đã hết hạn chờ thanh toán |
| `12` | Thẻ bị khóa |
| `13` | Sai mật khẩu xác thực giao dịch |
| `24` | Khách hàng hủy giao dịch |
| `51` | Tài khoản không đủ số dư |
| `65` | Tài khoản vượt quá hạn mức giao dịch |
| `75` | Ngân hàng thanh toán đang bảo trì |
| `79` | Giao dịch vượt quá số lần thanh toán cho phép |

---

## 🎯 BACKEND IMPLEMENTATION (Tiếp Theo)

Sau khi chạy SQL script, bạn cần:

### 1. **Tạo VNPayService.cs**
- Tạo URL thanh toán
- Verify hash từ VNPay
- Xử lý callback

### 2. **Tạo VNPayController.cs**
- `POST /api/vnpay/create-payment` - Tạo URL thanh toán
- `GET /api/vnpay/callback` - Nhận callback từ VNPay
- `GET /api/vnpay/transaction/{orderId}` - Lấy thông tin GD

### 3. **Tạo VNPayRepository.cs**
- Gọi các stored procedures
- CRUD VNPayTransactions

---

## ✅ CHECKLIST

### Database:
- [ ] Chạy `VNPAY_DATABASE_SETUP.sql` trong SSMS
- [ ] Verify table `VNPayTransactions` đã tạo
- [ ] Verify 6 stored procedures đã tạo
- [ ] Test stored procedures với data mẫu

### Config:
- [ ] Thêm VNPay config vào `appsettings.json`
- [ ] Đăng ký tài khoản VNPay Sandbox
- [ ] Lấy `TmnCode` và `HashSecret`

### Backend (Tiếp theo):
- [ ] Tạo `VNPayService.cs`
- [ ] Tạo `VNPayController.cs`
- [ ] Tạo `VNPayRepository.cs`
- [ ] Test API endpoints

### Frontend (Tiếp theo):
- [ ] Thêm nút "Thanh toán VNPay" vào trang Payment
- [ ] Xử lý redirect đến VNPay
- [ ] Xử lý callback return
- [ ] Hiển thị kết quả thanh toán

---

## 🔗 TÀI LIỆU THAM KHẢO

- **VNPay Sandbox**: https://sandbox.vnpayment.vn/
- **VNPay Documentation**: https://sandbox.vnpayment.vn/apis/docs/
- **Đăng ký Sandbox**: https://sandbox.vnpayment.vn/devreg/

---

## 📞 HỖ TRỢ

Nếu gặp vấn đề:
1. Check SQL error messages
2. Verify VNPay config
3. Check VNPay documentation
4. Test với thẻ test của VNPay

---

🎉 **Database đã sẵn sàng cho VNPay!** 🚀

**Bước tiếp theo**: Implement Backend (VNPayService, Controller, Repository)
